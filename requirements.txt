# Core Streamlit & Data Handling
streamlit>=1.30.0
pandas>=2.0.0
requests>=2.31.0
numpy>=1.26.0

# API Connectors (DataForSEO & OpenAI)
openai>=1.30.0
python-docx>=1.1.0

# Visualization (Cruciale per i grafici)
plotly>=5.18.0

# Advanced NLP/ML (Enterprise Features)
# Queste sono le librerie pesanti che hai controllato all'inizio del codice
scikit-learn>=1.3.0
networkx>=3.0
sentence-transformers>=2.2.0

# Topic Modeling (BERTopic dipende da queste)
# Sono necessarie per l'analisi Enterprise
bertopic>=0.15.0
umap-learn>=0.5.0
hdbscan>=0.8.0

# Optionali / Specifiche (Già usate nel codice)
# Sebbene siano moduli built-in o gestiti, è meglio includere i principali 
# che hai importato per chiarezza e compatibilità.
typing-extensions>=4.0.0
